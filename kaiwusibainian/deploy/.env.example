# 复制为 .env 后填写（.env 已被 .gitignore 排除，绝不提交到 GitHub）
# 密钥获取：控制台右上角 -> 账号中心 -> 访问管理 -> API 密钥管理（只显示一次）
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=

# COS 完整桶名（控制台「存储桶列表」可见，带 -APPID 后缀）
COS_BUCKET=
# 桶所在地域
COS_REGION=ap-hongkong

# CDN 加速域名（refresh_cdn.py 用），如 www.example.com
CDN_DOMAIN=
