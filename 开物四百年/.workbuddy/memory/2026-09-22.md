
# 2026-09-22

- 用户 GPT 无法访问公网 http://81.70.40.146:8100（GPT 抓取器拒非标端口/纯 IP + 强制 https），r.jina.ai 代理在其环境也被 DisabledError 禁掉。
- 我方实测：站点 200/0.03s；PowerShell 走 r.jina.ai 能取到文本（bash 沙箱连不上 r.jina.ai，需用 PowerShell Invoke-WebRequest）。
- 新增截图脚本 node workspace 下 capture-live.mjs / capture-live-teacher.mjs：对准公网地址拍 7 张（入口/首页/Walk/Craft/开物谱/Qa/教师端），输出到 NCDA\线上效果截图\。
- 坑：App.tsx 开屏 onDone 会把未登录 hash 强制改写 #/login，直接进 #/teacher 会被冲掉；先等登录页出现再 location.hash='#/teacher' 才能拍到教师端。
- 教师端截图含实时数据（9 会话/12 违动/16 答问），后端确认存活。
- r.jina 转取文本只有匿名壳（SPA 对匿名访客永远只渲染 Login），给 AI 看站内内容只能靠截图。
- 产出宣讲视频分镜脚本 Excel：`参赛物料\开物四百年_宣讲视频分镜脚本.xlsx`（2 表：分镜脚本 + 脚本说明）。9 镜约 2 分 46 秒，全程只演示平台（学校研学模式主线程），演示步骤按真实代码核对（开屏→登录→三模式→造物问"变"→现场观察点答错出补一环→课堂衔接→开物谱→问答证据约束→扣题）。台词无破折号/无反问开头/无防御表述；评分点五维对照（文化还原/艺术创新/技术融合/市场价值/可持续）。个人参赛无团队分工，视频禁学校/作者/教师/赛事 Logo。
- 建表走 tencent-docs-sheet-generation（openpyxl 直写）；bash 沙箱缺 coreutils，脚本用托管 python（versions\3.13.12\python.exe）+ PowerShell 跑，PowerShell stdout 被吞→输出写临时文件再 Read；本脚本无公式故跳过 recalc。
- P1 海报 HTML 精确排版：用真实素材（大赛 logo、项目 logo、系统截图、古今融合 AI 主视觉、真实二维码），避开 AI 重绘系统界面和二维码。A3 竖版 4961×3508 px/300DPI，输出 `参赛物料\P1-海报.jpg`（2.9 MB）与 PNG 预览。手机截图统一加样机边框；文案无破折号、无迂回/防御性表述。渲染脚本 `render-p1.mjs` 放在 node workspace，用 playwright-core 直驱本地 Chrome。
- Lightbox 复位按钮修复（用户反馈第三个双向箭头看不清且点击无反应）：`⤢` 字符换为四角框 SVG（feather maximize），缩放/复位按钮到边界时 disabled 置灰（.lb-btn:disabled opacity .35）。已 build:cos → tar → remote_update.py 部署公网（新 bundle index-DqVBu2T0.js，服务 active，/ 与 /api/meta 均 200），本地已补 plain build + 重建 dist\NCDA junction。SSH 密码可从历史会话检索（conversation_search 查 KAIWU_SSH_PW 命中，密码 Imay@1215）。改动未 commit（用户自行推送，提交前先问）。
- 首页页脚去掉文字标前的圆形 logo 图（Home.tsx 删 sf-mark img + logoUrl import，index.css 删 .sf-mark 规则）。同流程部署公网（bundle index-CE3TuTjZ.js，active/200），本地 plain build + junction 已重建。改动未 commit。
- 用户反馈"没去掉"实为浏览器缓存旧 bundle。根治：backend main.py 静态挂载前加 _cache_control 中间件（index.html→no-cache，/assets/*→immutable 一年），已部署验证生效。
- 撰写 80-100 字《开物四百年》设计说明（投稿表单用，与 docs/投稿清单与设计说明.md 里 430 字正式稿是两版）：99 字版被否，改用**命题对位版 ~200 字**（重写要点：紧扣"将自然之材转化为实用之器"造物观 + 新余钢铁产业链落点 + 方向3 数字智能原话"数据驱动的内容决策与服务模式创新" + 纯软件/可迭代/低成本复制到锂电新能源·装备制造 = 市场价值 + 收在高端化·智能化·绿色化·融合化转型与设计赋能）。规则仍旧：无破折号/无迂回立论/无防御表述。
- NCDA 天工开物杯命题硬信息（2026-09-22 用户提供）：六大产业=钢铁·锂电新能源·电子信息·装备制造·新材料·食品医药；三方向=先进制造/文创产品/数字智能（本品报数字智能）；评分五维=文化还原度·艺术创新性·技术融合·市场价值·可持续性；高校组每校限 15 件、个人/团队≤3 作者 2 指导教师；9-01~9-22 投稿、9-23~24 校方审核、10-01 公示、10-08 颁奖；高校组在 www.fd.show 提交；奖金高校组一/二/三等 2万·5千·2千。新增扣分项：地图不规范重大错误直接零分。
- **大坑自踩**：plain build（base=/NCDA/）后的 dist 被误打包部署，线上 js src 变 /NCDA/assets/… 直接 404。修正并固化为铁律：**每次 remote_update 前必须现跑 build:cos 再 tar，绝不能用本地 plain build 的 dist 打包**；plain build+junction 只在 deploy 完成之后做。已用 build:cos 重新构建部署（/assets/index-CE3TuTjZ.js，fetch 200，sf-mark 不存在，服务 active/200）。
- 参赛简介/设计说明四段文案的高亮方案（用户逐段来问：作品简介段、问题段、标志设计说明段、配色说明段）。确立三档体系并入 MEMORY.md。四段分工：简介段标主张与落点、问题段标问题定义、logo 段标"出处+可核验动作"、配色段标"语义映射端点"——同一套朱砂+古铜，跨段形成"主张响—问题准—做法实"的层次。
- 文案问题发现（用户原文）：①logo 段用了破折号"炉火正燃——这是…"，违反禁用破折号规矩，改句号后高亮句独立成块更好；②同段"赭金""暖金"两个叫法指同一色，建议统一为赭金。
- 配色核对结论（2026-09-22）：四段配文与代码实际逐条一致，可放心用于参赛材料——古=朱砂 #b8432c(--era-ancient) / 今=钢青 #3e4a57(--steel) / 赭金 #cfa96b(--st-exposed 已接触) / 古铜 #8c6a2f(--st-verified 已验证) / 纯白画布 #fff / 墨 #1c1915。发现两处可补强：**朱砂额外承担「理解断点」(--st-gap) 语义且与「古」同色值**（四百年前断掉的那一环与今天学生没弄懂的那一环是同一抹红，设计上最妙的一点，但配文只写了"古"这一层）；docs/海报文案库-设计规范与用户画像.md 里已有现成配文"你学到哪一步，颜色就氧化到哪一步"，比"暗合学习从接触到掌握的进阶"更适合展板口语。已修正 MEMORY.md 中"古站墨黑、今站朱砂"的错误记录。
