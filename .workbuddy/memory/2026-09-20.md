# 2026-09-20

## 开物脉络（kaiwu-mailuo）开屏动画 + 手机访问
- 修复 App.tsx 上个会话遗留 bug：`onDone` 里引用未定义的 `SPLASH_KEY`/`today`（限流中断导致），已删除该行。开屏逻辑本就是每次打开都播（`splashDone` 初始 false），无 sessionStorage/day 锁。
- Splash.tsx：画布加 `window.resize` 监听（fit 函数重设 canvas 尺寸 + setTransform(dpr)，并把越界粒子收回）；粒子数按屏幕面积缩放（36~70，手机省电）。
- index.css：`.splash-title` 字号改为 `clamp(30px, 11vw, 46px)`，`.splash-sub` `clamp(10px, 3vw, 13px)`，间距 clamp。
- 启动网站.bat：uvicorn 从 127.0.0.1 改绑 0.0.0.0，启动时用 PowerShell Get-NetIPAddress 自动取局域网 IP 并显示 `http://<IP>:8100` 供手机同一 Wi-Fi 访问；提示防火墙放行。
- 已 `npm run build` 重新构建前端（tsc -b 通过），FastAPI 8100 端口直接服务新 dist。
- 架构备忘：FastAPI(8100) 服务 frontend/dist 静态文件，/api 同源；Vite dev 代理 /api → 127.0.0.1:8100。手机公网访问需内网穿透或云部署（后端有 SQLite + deepseek 依赖，静态托管不够）。
- GitHub 仓库：https://github.com/Onesummer-D/NCDA（改动目前只在本地，未推送）。

## 现场文案润色方案落地（zcode 方案 → content.json + app.db）
- 执行 `kaiwu-mailuo/现场文案润色方案.md`：替换 11 个 process_nodes 的 summary+details、11 张 explanations 讲解卡正文；E05 备注改为"逐字引用《五金》原文"；M1/X9 官方产业链分隔符统一为顿号；M4 details 由 3 条扩为 4 条。
- 关键偏差：方案里的"新增 E07"实际被教育部研学文件占用 → 新华每日电讯改为 **E08**（A1/A3 evidence_ids、X3/X8/X11 source_refs 增引 E08）。
- 关键架构点：`seed.py` 仅在 nodes 表为空时灌库（幂等跳过）→ 直接改 content.json 不会生效。处理方式：脚本 `backend/apply_copy_20260920.py` 同时更新 content.json 和 app.db（原地 UPDATE，不删库、保留用户数据）。备份在 `backend/_backup_copy_20260920/`。
- 后端每请求直接查 SQLite，无缓存 → 改动即时生效，无需重启 bat；启动方式（双击 启动网站.bat）未做任何变更。

## 开物谱图表调整
- 字号调小：.g-name 15→13px、.g-id 14→11.5px、.g-era-label 14→12px。
- 配色定为「赭金古铜」方案（用户三选一）：新增状态变量 `--st-exposed:#cfa96b`（浅赭金·已接触）、`--st-verified:#8c6a2f`（古铜·已验证）、`--st-gap:#b8432c`（朱砂·断点），替换原钢青/橄榄绿。
- 关键区分：`--steel`/`--ok`/`--gap` 另有时代/对错等语义，未全局改动，只换"掌握状态"类引用（ch-state、s-dot、Spectrum STATE_COLOR、legend、pulse）。
- 顺带修复海报 bug：canvas fillStyle 不认 var()，原研学海报的圆点状态色一直失效；新增 STATE_HEX hex 映射给 canvas 用。
- 已重新 build。用户偏好：配色等视觉决策喜欢看对比图再选（AskUserQuestion + swatch widget 有效）。

## 三种参观方式的现状盘点（用户问差异）
- 设计文档（PDF p12）意图：casual=古今故事基础导览；curious=启动认知导航；school=课程目标+观察任务+教师端反馈，比赛 MVP 重点是 school。
- 代码实际差异仅 3 处：① 全站强调色随模式切换（casual 赭金/curious 黛蓝/school 朱砂）；② 仅 school 在现场页显示「课堂衔接」教材卡（Walk.tsx:200）；③ 后端推荐评分 school +0.5（main.py:212）。
- 观察任务/答题所有模式都能做；"casual 只给故事、curious 才有认知导航"的差异化尚未实现，是文档与实现的差距（潜在待办）。
- → 已实现分流：Walk.tsx 观察任务区与补一环推荐区对 casual 隐藏；Home「考考你」卡 casual 隐藏；Craft 想看懂模式默认落在「变」（工艺因果）视图（defaultView），补一环眉题在 curious 下显示"补一环 · 认知导航"。最终三模式差异：casual=纯故事导览；curious=因果视图+认知导航；school=再+课堂衔接+推荐加权。注意：模式以创建的 session 为准（点「进入」时），首页轮播"了解更多"未开会话时按默认 school。已 build。
- Walk「下一站」按钮原只覆盖今列（modernChain），古代 5 站没有；已改为沿整条工艺链顺序推进（A1→…→A5→M1→…→M5，末站 M6 无按钮）。
- 古今过渡特殊处理（用户指出 A5→M1 不该是普通"下一站"）：过渡按钮文案定为「穿越四百年 → 去看今天的开物脉络」（用户先选主题句版后改口）；现代站（era=modern）的「下一站」按钮改朱砂红 `.modern-next { background: var(--era-ancient) }`（用户选定，固定值不随模式 accent 变），古代站保持墨黑。
- 问答页返回入口：从造物问「再问一句」跳转（qaContext 非空）时，问个开物顶图左上角显示胶囊按钮「← 返回造物问」（.qa-back，白底圆角悬浮）；点击 setCraftQi(该题索引) + 清 qaContext + location.hash='#/craft'，回到刚才那道造物问（视图重置为默认）。直接进问答（无 qaContext）不显示。
- 文档页拆分：参观指南独立为 #/guide（原来和关于我们同指 #/about）。Doc.tsx 重写：about 去防御性表述改四条正面陈述（联系我们段已按用户要求删除）；guide 六段改五段（三模式分流逻辑、三条动线、边看边验证、开物谱海报、安全提示；"手机访问"段已删）。隐私政策/服务条款更新：terms 删安全段（与 guide 重复）。竞赛通知 PDF（4页）检索无"不得出现联系信息/署名"类条款。
- doc 页宽度修复：max-width 640→420px（与全站 .shell 一致），doc-brand 底部落款改为通栏水平居中。
- 登录页：老师卡描述删「理解断点」（改为"班级数据 · 教学反馈"），学生/老师两卡等高（auth-roles 加 align-items:stretch）。
- **品牌更名：开物脉络 → 开物四百年**（用户从候选中选定）。已替换平台所有品牌位：index.html title、Splash 开屏标题、Login logo/按钮、Home 页脚、Doc 四页、Teacher、海报文件名与分享文案（App/poster/Spectrum canvas）、开物谱 intro（该句非品牌用法改写为"工艺脉络"）、backend main.py QA 系统提示词与 FastAPI title、content.json meta.product、css/sql 注释。唯一保留：Walk.tsx:224 过渡按钮「穿越四百年 → 去看今天的开物脉络」（用户明确豁免）。boards/*.html、docs/*.md、README 等参赛物料未动（非平台运行时）。已 build。
